from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='The real world does not slow down for bad data',
    author='Ruturaj Kiran Vaidya',
    license='MIT',
)
